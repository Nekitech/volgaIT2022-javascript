import React from 'react'

const CounterContext = React.createContext(0)
const PageHistory = React.createContext()
const UserAnswer = React.createContext()
export {CounterContext, PageHistory, UserAnswer}
